import React from 'react'

export const Header: React.FC = () => {
    return <header className="header">
        <h1>VRChat NameTag Generator</h1>
        <p>
            input your name and name tag color
        </p>
        <p>
            you can save image with right click(desktop Windows) or long touch(phone) or two finger tap(MacOS, laptop)
        </p>
    </header>
}
